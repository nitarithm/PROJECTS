/**
 * Given a number n, square it.
 *
 * @param {number} n 
 * @return {number} n squared
 */
function squared(n) {
    const result = n * n;

    return result;
}

module.exports = squared;